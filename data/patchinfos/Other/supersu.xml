<patchinfo>
    <name>Chainfire's SuperSU</name>
    <regex>^(UPDATE|BETA)-SuperSU-v[0-9A-Za-z\.]+\.zip$</regex>

    <has-boot-image>false</has-boot-image>
    <!-- Doesn't need any patches! :) -->
</patchinfo>
