namespace mbtool.daemon.v3;

table PathCopyRequest {
    // Path to source file
    source : string;
    // Path to destination file
    target : string;
}

table PathCopyResponse {
    success : bool;
    error_msg : string;
}