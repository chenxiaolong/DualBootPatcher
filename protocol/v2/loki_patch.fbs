namespace mbtool.daemon.v2;

table LokiPatchRequest {
    // Path to source boot image file
    source : string (deprecated);
    // Path to destination boot image file
    target : string (deprecated);
}

table LokiPatchResponse {
    success : bool (deprecated);
}