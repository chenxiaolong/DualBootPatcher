namespace mbtool.daemon.v2;

table CopyRequest {
    // Path to source file
    source : string;
    // Path to destination file
    target : string;
}

table CopyResponse {
    success : bool;
    error_msg : string;
}