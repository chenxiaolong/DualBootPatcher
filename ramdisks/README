Precompiled binaries:

* init-kk44
  - This is the Android 4.4 Kit Kat init binary. To rebuild, run:

    $ repo init -u git://github.com/CyanogenMod/android.git -b cm-11.0
    $ repo sync -c
    $ . build/envsetup.sh
    $ make init

* init
  - This is the Android 4.3 Jelly Bean init binary. To rebuild, run:

    $ repo init -u git://github.com/CyanogenMod/android.git -b cm-10.2
    $ repo sync -c
    $ . build/envsetup.sh
    $ make init
